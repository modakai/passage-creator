# 测试环境配置文件
# @author sakura
# @from sakura
server:
  port: 8102

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sakura_passage_creator_test
    username: root
    password: 123456
  data:
    redis:
      database: 2
      host: localhost
      port: 6379
      timeout: 5000
      password:

mybatis-flex:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
