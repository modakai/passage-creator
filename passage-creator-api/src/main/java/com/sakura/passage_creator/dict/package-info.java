/**
 * 字典模块，负责字典类型、字典项维护和跨模块字典校验能力。
 */
@ApplicationModule(
        displayName = "字典模块",
        allowedDependencies = "shared"
)
package com.sakura.passage_creator.dict;

import org.springframework.modulith.ApplicationModule;
