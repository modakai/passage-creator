## 1. 后端数据模型与迁移

- [x] 1.1 新增提示词反馈表迁移脚本，包含用户、任务、环节、评价、备注、提示词使用日志和模板版本快照字段
- [x] 1.2 为反馈表添加 `user_id + task_id + feedback_stage` 唯一约束，以及环节、评价、模板标识、创建时间查询索引
- [x] 1.3 新增反馈环节枚举和反馈评价枚举，覆盖 `TITLE_SELECTION`、`OUTLINE_EDITING`、`CONTENT_MERGED` 与 `VERY_SATISFIED`、`SATISFIED`、`NEUTRAL`、`UNSATISFIED`
- [x] 1.4 新增提示词反馈实体、Mapper、DTO、VO 和查询请求对象，并为关键字段添加必要注释

## 2. 后端反馈业务与接口

- [x] 2.1 实现用户端反馈提交服务，校验登录用户、任务归属、环节枚举、评价枚举和备注长度
- [x] 2.2 实现同一用户、同一任务、同一环节的 upsert 逻辑，重复提交时更新最终有效反馈
- [x] 2.3 实现反馈到 Prompt 使用日志的关联解析，保存模板 ID、模板标识、版本和环境快照，无法关联时允许落库
- [x] 2.4 新增用户端反馈提交接口，接口失败不得影响文章创作流程状态
- [x] 2.5 新增管理端反馈记录分页接口，支持按环节、评价、模板标识、版本和时间范围筛选
- [x] 2.6 新增管理端反馈统计接口，按已提交反馈返回四档满意度数量、总数和占比
- [x] 2.7 为非管理员访问管理端反馈接口补齐权限校验

## 3. 前端创作页反馈入口

- [x] 3.1 新增提示词反馈 API 封装和类型定义
- [x] 3.2 新增可复用的弱提示反馈组件，支持四档满意度选择、关闭、提交中和提交失败状态
- [x] 3.3 在标题生成完毕待选择阶段展示 `TITLE_SELECTION` 反馈入口，且不阻塞标题选择
- [x] 3.4 在大纲生成完毕待编辑阶段展示 `OUTLINE_EDITING` 反馈入口，且不阻塞大纲编辑和确认
- [x] 3.5 在正文融合完成阶段展示 `CONTENT_MERGED` 反馈入口，且不阻塞最终内容查看或保存结果展示
- [x] 3.5.1 在 rednote 生成完成后展示同款反馈入口，一次点击同步提交 `REDNOTE_CONTENT`、`REDNOTE_NORMAL_IMAGE_PROMPT`、`REDNOTE_COVER_IMAGE_PROMPT`
- [x] 3.6 确保同一环节反馈提交成功后当前页面不重复打扰用户

## 4. 管理后台反馈记录与统计

- [x] 4.1 新增管理后台提示词反馈页面，展示环节维度四档满意度数量和占比
- [x] 4.2 新增反馈记录表格，展示用户、任务、环节、评价、备注、模板标识、版本和时间信息
- [x] 4.3 新增筛选条件，支持环节、评价、模板标识、版本和时间范围筛选
- [x] 4.4 在管理端侧边栏 Prompt 管理附近加入提示词反馈入口
- [x] 4.5 对未关联到 Prompt 使用日志的记录提供明确标识，避免管理员误判为模板统计缺失

## 5. 验证

- [x] 5.1 增加后端服务层或接口层测试，覆盖提交反馈、重复提交更新、非法环节拒绝、非任务拥有者拒绝和管理权限校验
- [x] 5.2 增加统计逻辑测试，覆盖跳过反馈不计入分母和按环节/模板版本筛选占比
- [x] 5.3 增加前端关键逻辑测试或临时验证，覆盖三处反馈入口的展示条件和提交失败不阻塞流程
- [x] 5.4 运行后端相关测试与前端类型检查/构建，确认创作主流程和后台页面未被破坏
