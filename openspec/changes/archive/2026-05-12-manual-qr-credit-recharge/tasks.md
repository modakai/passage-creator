## 1. 数据库与后端配置

- [x] 1.1 在 MySQL 初始化脚本中新增 `credit_recharge_application` 表，包含申请号唯一索引、用户/时间索引和状态/时间索引
- [x] 1.2 在 PostgreSQL 初始化脚本中新增同等结构的 `credit_recharge_application` 表和索引
- [x] 1.3 在 `billing` 模块新增人工充值配置类，定义启用套餐、微信收款码地址和支付宝收款码地址
- [x] 1.4 在应用配置中补充默认内测套餐：10 元 100 积分、30 元 350 积分、50 元 650 积分，并使用本地静态资源路径作为收款码默认值

## 2. 后端领域模型

- [x] 2.1 新增 `CreditRechargeApplication` 实体，字段覆盖申请号、用户、套餐、金额、积分、支付方式、状态、用户备注、管理员备注、审核信息和逻辑删除
- [x] 2.2 新增 `CreditRechargeApplicationMapper` 并接入 MyBatis-Flex 持久化
- [x] 2.3 新增支付方式枚举 `WECHAT`、`ALIPAY`、`UNKNOWN` 和申请状态枚举 `PENDING`、`APPROVED`、`REJECTED`
- [x] 2.4 新增用户端创建申请、查询申请、管理端查询和审核请求 DTO，确保创建申请 DTO 不包含金额、积分和申请号
- [x] 2.5 新增套餐、收款信息、申请详情和申请列表 VO，返回前端所需状态、备注、时间和收款码信息

## 3. 后端服务实现

- [x] 3.1 新增 `ManualRechargeApplicationService` 接口，定义查询套餐、创建申请、用户查询、管理端分页、审核通过和审核拒绝方法
- [x] 3.2 实现套餐解析逻辑，只允许从后端配置读取金额和积分，并过滤未启用套餐
- [x] 3.3 实现创建申请逻辑，由后端生成唯一 `rechargeNo`，保存套餐金额和积分快照，初始状态为 `PENDING`
- [x] 3.4 实现用户查询逻辑，所有列表和详情查询必须强制绑定当前登录用户
- [x] 3.5 实现管理端分页查询逻辑，支持按 `PENDING`、`APPROVED`、`REJECTED` 状态筛选
- [x] 3.6 实现审核通过逻辑，在同一事务内完成 `PENDING` 条件更新和调用积分充值逻辑，确保重复审核不会重复加积分
- [x] 3.7 如现有 `CreditAccountService.recharge(...)` 不便关联申请号，则新增内部充值命令或重载方法，用 `MANUAL_RECHARGE` 和 `rechargeNo` 记录积分流水
- [x] 3.8 实现审核拒绝逻辑，只允许拒绝 `PENDING` 申请，并写入拒绝原因、审核人和审核时间

## 4. 后端接口

- [x] 4.1 在用户端积分 Controller 新增查询套餐接口
- [x] 4.2 在用户端积分 Controller 新增创建人工充值申请接口
- [x] 4.3 在用户端积分 Controller 新增当前用户充值申请分页和详情接口
- [x] 4.4 在管理端积分 Controller 新增充值申请分页查询接口，并添加管理员权限校验
- [x] 4.5 在管理端积分 Controller 新增审核通过接口，并添加管理员权限校验和操作审计
- [x] 4.6 在管理端积分 Controller 新增审核拒绝接口，并添加管理员权限校验和操作审计

## 5. 前端接口与类型

- [x] 5.1 在 `credit.type.ts` 新增人工充值套餐、申请、状态、创建申请和审核请求类型
- [x] 5.2 在 `credit.type.ts` 新增 `PENDING`、`APPROVED`、`REJECTED` 状态中文标签转换函数
- [x] 5.3 在 `credit.api.ts` 新增用户端套餐查询、创建申请、申请分页和详情接口封装
- [x] 5.4 在 `credit.api.ts` 新增管理端申请分页、审核通过和审核拒绝接口封装，并设置相关 query 失效刷新

## 6. 用户端页面

- [x] 6.1 在 `billing/index.vue` 新增人工充值套餐选择区域，沿用现有卡片、按钮和提示组件
- [x] 6.2 用户选择套餐并创建申请后，展示金额、积分、充值申请号、微信收款码和支付宝收款码
- [x] 6.3 在付款信息区域明确展示“扫码付款后请在付款备注中填写充值申请号：xxxx”和人工审核非实时到账提示
- [x] 6.4 在 `billing/index.vue` 新增用户申请列表，展示待审核、已到账、已拒绝状态和拒绝原因
- [x] 6.5 确保页面不出现“支付成功自动到账”或等价误导文案

## 7. 管理端页面

- [x] 7.1 在 `credit-management/index.vue` 新增人工充值申请管理区，保留现有管理员直接充值入口
- [x] 7.2 新增状态筛选控件，支持查看全部、待审核、已到账和已拒绝申请
- [x] 7.3 在申请表格中展示用户 ID、申请号、金额、积分、支付方式、状态、用户备注、管理员备注和时间
- [x] 7.4 对 `PENDING` 申请提供审核通过和审核拒绝操作，拒绝时要求填写原因
- [x] 7.5 审核操作成功后刷新申请列表、积分账户和积分流水

## 8. 测试与验证

- [x] 8.1 补充后端核心服务测试：创建申请时金额和积分来自套餐配置，而不是前端输入
- [x] 8.2 补充后端核心服务测试：审核通过后用户积分余额增加，并生成可关联申请号的充值流水
- [x] 8.3 补充后端核心服务测试：已审核申请重复通过不会重复增加积分
- [x] 8.4 补充后端核心服务测试：拒绝后的申请不能再审核通过
- [x] 8.5 运行后端相关测试；如新增或调整模块依赖，运行 `ModulithArchitectureTest`
- [x] 8.6 运行前端 `pnpm lint:fix` 和 `pnpm build`，确认类型和构建通过
- [x] 8.7 手动检查用户端和管理端文案，确认只表达人工审核、非实时到账，不暗示自动到账
