# 用户端独立项目 Todo

- [x] 创建独立用户端项目目录，记录迁移目标与任务拆分。
- [x] 搭建 Vue3 + TypeScript + Tailwind CSS 基础工程。
- [x] 按 `docs/ui/preview.html` 落地 Apple 风格 AI 创作首页。
- [x] 实现从首页一句话进入完整创作流程的路由与状态传递。
- [x] 独立迁移文章创作流程，突出标题确认、大纲确认、正文生成与配图结果。
- [x] 独立迁移小红书创作流程，突出素材检索、文案生成、图片规划与图片结果。
- [x] 迁移作品/任务/额度/个人中心的用户端页面，避免后台管理式表格体验。
- [x] 编写 `task.md` 记录当前后端暂不支持或可后续增强的 UI 功能。
- [x] 运行构建验证，确认无临时测试文件进入提交。
- [x] 按阶段提交 Git 版本。

## 后端接口与认证补齐

- [x] 核对后端 `/user` 认证接口、token header 和 SSE 鉴权方式。
- [x] 新增登录/注册页，并在注册成功后自动登录。
- [x] 新增本地会话保存、刷新校验、退出登录和路由守卫。
- [x] 将普通 API 请求统一接入后端 token header 与 401 处理。
- [x] 将文章/小红书 SSE 改为 fetch 流式读取，支持携带鉴权头。
- [x] 额度页接入 `/app/credit/summary` 后端接口。
- [x] 运行构建和浏览器验证后提交 Git。

## 认证页体验优化

- [x] 参考 `passage-creator` 登录注册页的 shell、hero、form 分层方式。
- [x] 抽取用户端认证页组件，减少页面内堆叠模板。
- [x] 保持独立用户端 Apple 风格 AI 创作工具视觉，不回退成后台/CMS 登录页。
- [x] 验证登录/注册切换、注册确认密码显示和构建结果。

## 作品卡片优化

- [x] 抽取作品卡片组件，避免作品页继续堆叠卡片模板。
- [x] 接入文章和小红书封面字段，封面不存在时保留轻量占位态。
- [x] 保持卡片式作品浏览体验，不回退成 CMS 表格。
- [x] 运行构建验证组件类型和页面引用。

## 小红书完成态布局优化

- [x] 创作完成后隐藏左侧“创作需求”输入卡，让结果内容占据主区域。
- [x] 将流程步骤改为 hugeicons 图标化展示，减少数字卡片的后台感。
- [x] 新增本地 HugeIcon 渲染组件，复用 hugeicons free icon 数据并保持类型检查。
- [x] 运行构建验证依赖、组件和页面布局引用。
- [x] 去除流程区渐变、编号和卡片包装，改成节点进度线连接。
